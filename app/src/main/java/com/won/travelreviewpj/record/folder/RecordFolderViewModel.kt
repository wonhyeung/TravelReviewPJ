package com.won.travelreviewpj.record.folder

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class RecordFolderViewModel(application: Application) : AndroidViewModel(application) {
}